# Milestone 11: Twinwright Bench plan

Spec: `design/superpowers/specs/2026-09-25-bench-design.md`. Worktree `.worktrees/milestone-11-bench` from `milestone-10-providers` (PR 9 tip) while that PR is open. Target main once PR 9 merges, or keep the stacked base.

## Task 1: suite parser

- Files: `internal/bench/suite.go`, `suite_test.go`.
- Strict version 1 YAML, category allowlist, path containment under `examples/`, digest of canonical body.
- Commit: `Add Twinwright Bench suite parser`

## Task 2: case runner and report

- Files: `internal/bench/run.go`, `run_test.go`, `report.go`.
- Execute cases with isolated DBs, judge via assertions or scenario eval, aggregate dimension rates and medians.
- Commit: `Run benchmark suites into JSON reports`

## Task 3: standard suite content

- Files: `examples/bench/standard.yaml` plus any small shared assertion snippets required.
- Curated cases across all six categories using existing scenarios/chaos/auth. Honest count in comments/README.
- Commit: `Add the standard Twinwright Bench suite`

## Task 4: CLI bench, compare reports, docs

- Files: `cmd/twinwright/main.go`, tests, README, ADR 0014.
- `bench` command; `compare` accepts two report JSON files; smoke the suite with scripted agent.
- Commit: `Expose twinwright bench and report comparison`
